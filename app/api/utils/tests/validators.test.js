const validators = require('../validators')

describe('Validators utils', () => {

  describe('areMinMaxValidator', () => {
    test('Should return false if a param isnt Number')
    test('Should return false if missing param')
    test('Should return false if first > last')
    test('Should return true')
  })

})